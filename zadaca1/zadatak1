def funkcija1(x):
    if not all(isinstance(y, str) for y in x):
        raise ValueError("Svi elementi moraju biti string!")
    return [y for y in x if len(y) > 4]

try:
    print(funkcija1(["Pas", "Macka", "Stol"]))
except ValueError as e:
    print(e)
